<?xml version="1.0"?>
<ruleset name="WordPress Plugin Coding Standards Configuration">
	<!-- Set a description for this ruleset. -->
	<description>Code standard rules to check against a WordPress Plugin.</description>

	<exclude-pattern>tests/*</exclude-pattern>

	<!-- Include the WordPress ruleset, with exclusions. -->
	<rule ref="WordPress-VIP">
		<!-- These may need to be excluded if they throw too many false positives -->
		<!-- <exclude name="Generic.WhiteSpace.ScopeIndent.Incorrect" /> -->
		<!-- <exclude name="Generic.WhiteSpace.ScopeIndent.IncorrectExact" /> -->
		<!-- <exclude name="PEAR.Functions.FunctionCallSignature.Indent" /> -->
	</rule>

	<!-- Exceptions for allowed media sideloading -->
	<rule ref="WordPress.VIP.FileSystemWritesDisallow.file_ops_unlink">
		<exclude-pattern>modules/class-simplechart-save.php</exclude-pattern>
		<exclude-pattern>modules/class-simplechart-wp-cli.php</exclude-pattern>
	</rule>

	<rule ref="WordPress.VIP.FileSystemWritesDisallow.FileWriteDetected">
		<exclude-pattern>modules/class-simplechart-save.php</exclude-pattern>
		<exclude-pattern>modules/class-simplechart-wp-cli.php</exclude-pattern>
	</rule>

	<rule ref="WordPress.Files.FileName.InvalidClassFileName">
		<exclude-pattern>simplechart.php</exclude-pattern>
		<exclude-pattern>modules/class-simplechart-insert.php</exclude-pattern>
	</rule>

	<rule ref="Squiz.Commenting.LongConditionClosingComment.Missing">
		<exclude-pattern>modules/class-simplechart-insert.php</exclude-pattern>
	</rule>

	<!-- Tweak the VIP rules -->
	<rule ref="WordPress.Security.EscapeOutput.OutputNotEscaped">
		<type>warning</type>
	</rule>

	<!-- https://github.com/WordPress-Coding-Standards/WordPress-Coding-Standards/issues/172 -->
	<rule ref="WordPress.VIP.ValidatedSanitizedInput.MissingUnslash">
		<exclude-pattern>modules/class-simplechart-save.php</exclude-pattern>
	</rule>
	<rule ref="WordPress.Security.ValidatedSanitizedInput.MissingUnslash">
		<exclude-pattern>modules/class-simplechart-save.php</exclude-pattern>
	</rule>

</ruleset>